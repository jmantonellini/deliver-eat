import { colors } from "../../constants/Styles";

export default {
  screen: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: colors.background,
    minHeight: "100vh",
  },
};
