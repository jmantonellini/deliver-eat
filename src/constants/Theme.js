import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#63d471",
      light: "#63a46c",
    },
    secondary: {
      main: "#69fff1",
    },
  },
});

export default theme;
